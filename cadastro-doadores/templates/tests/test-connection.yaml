apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cadastro-doadores.fullname" . }}-test-connection"
  labels:
    {{- include "cadastro-doadores.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['backend:{{ .Values.backend.service.port }}']
  restartPolicy: Never
